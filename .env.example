# Optional Configuration for Database Features
# The app works WITHOUT these - it will use localStorage mode
# Add these only if you want database sync, RAG, etc.

# Supabase Configuration (Optional - for cross-device sync)
# Get these from: https://supabase.com → Your Project → Settings → API
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# OpenAI Configuration (Optional - for RAG embeddings)
# Get from: https://platform.openai.com/api-keys
VITE_OPENAI_API_KEY=your_openai_api_key

# GitHub OAuth (Optional - for additional GitHub API calls)
# The app uses Supabase's GitHub OAuth, this is just for extra features
VITE_GITHUB_CLIENT_ID=your_github_oauth_client_id

# Note: Without configuration, the app runs in "localStorage mode"
# - Full chat functionality works
# - History saved in browser
# - No cross-device sync
# - Perfect for testing and personal use!
